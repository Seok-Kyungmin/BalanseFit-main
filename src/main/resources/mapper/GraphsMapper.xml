<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.balansefit.persistance.mapper.NutrientGraphsMapper">

<!--    <select id="sumCalories" resultType="int">-->
<!--        SELECT-->
<!--    </select>-->
    <!-- 몸무게 카운트-->
    <select id="countWeight" resultType="UserWeightDTO">
        SELECT CURRENT_WEIGHT FROM USER_WEIGHT
        WHERE
    </select>

</mapper>